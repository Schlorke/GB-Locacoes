# Configurações do npm para evitar warnings do pnpm
# Essas configurações são ignoradas pelo pnpm mas evitam warnings do npm

# Desabilitar warnings de configurações desconhecidas
fund=false
audit=false

# Configurações de compatibilidade
legacy-peer-deps=true

# Suprimir warnings específicos do pnpm
loglevel=error