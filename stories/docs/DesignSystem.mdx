import { Meta } from '@storybook/blocks'

<Meta title="Design System/Tokens & Guidelines" />

# GB Locações — Design System Overview

Sistema de design completo da GB Locações baseado em tokens semânticos e
componentes consistentes.

## 🎨 Paleta de Cores Primárias

### Cores da Marca

- **Primary**: `#ea580c` (Orange 600) — Cor principal da marca
- **Primary Light**: `#fed7aa` (Orange 200) — Backgrounds claros
- **Primary Dark**: `#f97316` (Orange 500) — Hover states
- **Secondary**: `#334155` (Slate 700) — Top bar, elementos secundários
- **Info**: `#3b82f6` (Blue 500) — Estados informativos

### Cores Semânticas

- **Success**: `#10b981` (Emerald 500)
- **Warning**: `#f59e0b` (Amber 500)
- **Error**: `#ef4444` (Red 500)

### Backgrounds

- **Primary**: `#f8fafc` (Slate 50) — Background principal
- **Secondary**: `#dbeafe` (Blue 50) — Background alternativo
- **Card**: `rgba(255, 255, 255, 0.95)` — Cards com transparência

## 🔤 Sistema Tipográfico

### Fontes

- **Sans**: `Inter` — Corpo de texto, interface
- **Heading**: `Jost` — Títulos, headings principais

### Escalas Responsivas

```css
/* Definidas com clamp() para responsividade */
h1: clamp(2.5rem, 5vw, 3.5rem)    /* 40px - 56px */
h2: clamp(2rem, 4vw, 3rem)        /* 32px - 48px */
h3: clamp(1.5rem, 3vw, 2.25rem)   /* 24px - 36px */
base: clamp(1rem, 2vw, 1.125rem)  /* 16px - 18px */
small: clamp(0.875rem, 1.5vw, 1rem) /* 14px - 16px */
```

### Line Heights

- **Tight**: 1.2 (títulos principais)
- **Normal**: 1.3 (subtítulos)
- **Relaxed**: 1.4 (parágrafos curtos)
- **Loose**: 1.6 (textos longos)

## 📏 Sistema de Espaçamento

Baseado em múltiplos de 4px:

- **xs**: `0.25rem` (4px)
- **sm**: `0.5rem` (8px)
- **md**: `1rem` (16px)
- **lg**: `1.5rem` (24px)
- **xl**: `2rem` (32px)
- **2xl**: `3rem` (48px)

## 🎯 Border Radius

- **sm**: `8px` — Inputs, small buttons
- **md**: `12px` — Cards secundários
- **lg**: `16px` — Cards principais, CTAs

## ✨ Shadows (Elevação)

```css
--shadow-card:
  0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
--shadow-modal: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
--shadow-button:
  0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
--shadow-overlay: 4px 8px 18px 2px rgba(0, 0, 0, 0.18);
```

## 🎭 Sistema de Animações

### Durações

- **Fast**: `0.2s` — Micro-interações
- **Normal**: `0.3s` — Hover, focus states
- **Slow**: `0.8s` — Entrance animations

### Easings

- **ease-out**: Animações de entrada
- **ease-in**: Animações de saída
- **ease-in-out**: Transições bidirecionais

### Animações Signature

```css
/* Floating elements */
@keyframes float {
  0%,
  100% {
    transform: translateY(0px);
  }
  50% {
    transform: translateY(-20px);
  }
}

/* Scale entrance */
@keyframes scaleIn {
  0% {
    transform: scale(0.95);
    opacity: 0.8;
  }
  100% {
    transform: scale(1);
    opacity: 1;
  }
}

/* Slide up entrance */
@keyframes slideInUp {
  0% {
    opacity: 0;
    transform: translateY(60px);
  }
  100% {
    opacity: 1;
    transform: translateY(0);
  }
}
```

## 🧩 Componentes Base

### Button Variants

- **default**: Primary color, used for main CTAs
- **destructive**: Red background, for destructive actions
- **outline**: Transparent with border, secondary actions
- **secondary**: Gray background, alternative actions
- **ghost**: No background, minimal style
- **link**: Looks like a link, inline usage

### Card Structure

```tsx
<Card className="relative overflow-hidden border-0 shadow-xl bg-white backdrop-blur-sm hover:shadow-2xl transition-all duration-500 hover:scale-105 group">
  <CardContent className="p-6 relative z-10">{/* Content */}</CardContent>
  {/* Bottom accent line */}
  <div className="absolute bottom-0 left-0 w-full h-1 bg-gradient-to-r from-orange-500 to-yellow-500 transform scale-x-0 group-hover:scale-x-100 transition-transform duration-500 z-0"></div>
</Card>
```

## 🎪 Padrões de Hover

### Cards

```css
.card-hover {
  @apply shadow-xl hover:shadow-2xl hover:scale-105;
  @apply transition-all duration-500;
}
```

### Buttons

```css
.button-hover {
  @apply hover:scale-105 hover:shadow-lg;
  @apply transition-all duration-300;
}
```

### Icons

```css
.icon-hover {
  @apply group-hover:scale-110 group-hover:rotate-12;
  @apply transition-all duration-500;
}
```

## 🌐 Layout System

### Container

```css
.container-main {
  @apply max-w-7xl mx-auto px-4 sm:px-6 lg:px-8;
}
```

### Grid Responsivo Padrão

```css
.grid-responsive {
  @apply grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8;
}
```

## ⚠️ Regras Críticas (Anti-alucinação)

### ✅ SEMPRE FAZER

1. Usar tokens de cor definidos (`#ea580c`, `#3b82f6`)
2. Aplicar fonts corretas (Inter/Jost)
3. Manter espaçamentos consistentes (múltiplos de 4px)
4. Usar componentes do design system
5. Aplicar hover effects padronizados
6. Respeitar hierarchy tipográfica

### ❌ NUNCA FAZER

1. **NUNCA** inventar cores fora da paleta
2. **NUNCA** usar fonts diferentes de Inter/Jost
3. **NUNCA** quebrar a responsividade
4. **NUNCA** remover animações sem `prefers-reduced-motion`
5. **NUNCA** alterar a estrutura de cards padronizada
6. **NUNCA** ignorar acessibilidade (ARIA, contrast, focus)

## 🧪 Testing & Validation

### Visual Regression

- Todas as seções têm snapshots no Chromatic
- Breakpoints testados: 375px, 768px, 1024px, 1440px

### Acessibilidade

- WCAG 2.1 AA compliance
- Color contrast min 4.5:1
- Keyboard navigation funcional
- Screen reader compatibility

### Performance

- Core Web Vitals otimizados
- Animações respeitam `prefers-reduced-motion`
- Images otimizadas com Next.js Image
